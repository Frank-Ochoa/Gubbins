arr ( (int a) -> int ) functions := arr ( (int a) -> int ) (10);

int b := 20;

for i in range 10
{
    functions[i] := func (int a) -> int
    {
        return a + i + b;
    };
}

b := 500;

(int a) -> int x1 := functions[0];

(int a) -> int x2 := functions[1];

print x1(10);

print x2(10);